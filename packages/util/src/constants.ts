import { getWebUrl, environment, getEditorUrl } from './utils/component'

export const WEB_URL = getWebUrl(environment)
export const EDITOR_URL = getEditorUrl(environment)
export const SUPPORT_EMAIL = 'support@harmonyui.dev'

export const DEFAULT_WIDTH = 1960
export const DEFAULT_HEIGHT = 1080
export const INDEXING_VERSION = '0.0.3'
